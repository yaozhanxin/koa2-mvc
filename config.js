const config = {

    // 项目启动端口
    port: 3000,
  
    database: {
      DATABASE: 'record_mysql',
      USERNAME: 'root',
      PASSWORD: 'root',
      HOST: '127.0.0.1'
    }
  }
  
  module.exports = config